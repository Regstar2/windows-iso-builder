<div align="center">

# Windows ISO Builder

Интерактивный клиент UUP dump для поиска, загрузки и автоматической сборки Windows ISO без ручной работы с сайтом, UUID, SKU и `ConvertConfig.ini`.

**Русский** · [English](README_EN.md)

[Быстрый старт](#быстрый-старт) ·
[Документация](#документация) ·
[Ограничения](#ограничения)

</div>

## О проекте

Windows ISO Builder проводит пользователя через поиск сборки Windows, выбор архитектуры, языка, редакций и формата установочного образа. Каталог версий не хранится в исходном коде: данные запрашиваются у UUP dump, а файлы Windows загружаются с Microsoft Windows Update/CDN.

Техническое имя репозитория и корневого каталога — `windows-iso-builder`. Публичное название продукта остаётся Windows ISO Builder.

Проект не заменяет UUP dump и не реализует собственный UUP-движок. Его задача — сократить ручной процесс до одного интерактивного сценария.

## Статус проекта

Текущая версия — `0.1.0-alpha.1`. Код находится на стадии закрытой alpha-проверки.

Статическая структура и модульная архитектура подготовлены, но полный цикл создания ISO ещё не подтверждён на реальных сборках Windows 10 и Windows 11. До такой проверки проект не считается готовым к публичному стабильному релизу.

## Возможности

- динамический поиск по названию релиза, номеру сборки или заголовку UUP dump;
- постраничный просмотр больших результатов поиска с переходом вперёд, назад и к нужной странице;
- отсутствие фиксированного каталога поддерживаемых версий Windows;
- фильтры `x64`, `ARM64` и `x86`, Preview/Insider скрыты по умолчанию;
- динамическая загрузка языков и редакций выбранной сборки;
- выбор одной или нескольких редакций через virtual editions конвертера UUP dump;
- создание `install.esd` или `install.wim`;
- кеширование API, конвертационного пакета и UUP-файлов;
- продолжение загрузок через `aria2` в постоянном рабочем каталоге;
- журнал, SHA-256 и JSON-метаданные результата;
- проверка структуры ISO через `Mount-DiskImage` и DISM;
- интерактивный TUI и неинтерактивный режим PowerShell.

## Быстрый старт

1. Распакуйте проект в локальный каталог.
2. Запустите `Start-Builder.cmd`.
3. Выберите поиск и создание ISO.
4. Введите запрос, например `22H2` или `19045`.
5. Выберите сборку, архитектуру, язык, редакции и формат.
6. Подтвердите UAC перед загрузкой и конвертацией.

Готовые файлы сохраняются в `output/`. Постоянный рабочий кеш по умолчанию расположен в `C:\UUP-ISO-Work`.

## Требования

- Windows 10 или Windows 11 x64;
- Windows PowerShell 5.1 или PowerShell 7;
- права администратора на этапе загрузки и конвертации;
- не менее 35–50 ГБ свободного места;
- доступ к UUP dump и Microsoft Windows Update/CDN.

## Использование

В интерактивном режиме программа сначала собирает параметры и только затем запрашивает повышение прав. Это позволяет отменить или изменить выбор до начала длительной операции.

Каталог различает полноценные записи Windows и служебные пакеты обновлений. По умолчанию `.NET`, cumulative, OOBE и другие servicing-записи скрыты, чтобы их нельзя было случайно выбрать как источник ISO. В меню сортировки доступен столбец `Тип записи`; полноценные сборки Windows выводятся перед обновлениями. Это классификация каталога, а не гарантия того, что Microsoft всё ещё хранит UUP-файлы конкретной старой сборки.

Повторный запуск с той же сборкой, языком и базовой редакцией использует существующий рабочий каталог. `aria2` проверяет загруженные файлы и продолжает незавершённую загрузку, если это поддерживается сервером.

В таблице результатов доступны команды `S` для сортировки по актуальности, сборке, архитектуре, дате, типу записи или названию и `F` для выбора актуальной установочной сборки. Режим «Актуальность» сначала сравнивает номер выпуска Windows (например, 22H2 новее 21H2 и 1809), затем выбирает максимальную сборку внутри этого выпуска. Поэтому старая LTSC-ветка с более свежей датой обслуживания и старый development build с большим номером не считаются текущим релизом. При сортировке по типу можно вывести сначала полноценные сборки Windows или сначала служебные обновления. Направление сортировки сохраняется при перелистывании страниц.

## Команды

Неинтерактивный пример:

```powershell
powershell.exe -NoProfile -ExecutionPolicy Bypass -File .\Start-Builder.ps1 `
  -NonInteractive `
  -Search 22H2 `
  -Architecture amd64 `
  -Language ru-ru `
  -Editions Core,Professional `
  -ImageFormat ESD
```

В этом режиме при наличии подходящих записей предпочтение отдаётся полноценной сборке Windows, а не cumulative/.NET/OOBE-пакету. Прямой выбор UUID пока не реализован.

## Архитектура

PowerShell-модуль разделяет работу с API, кешем, выбором параметров, формированием плана и запуском конвертера. Подробное описание находится в [документе архитектуры](docs/ARCHITECTURE.md).

## Безопасность

Программа не должна отключать антивирус, UAC или глобальную политику выполнения PowerShell. Пакет UUP dump запускается только после проверки структуры ZIP и распаковывается в принадлежащий программе рабочий каталог.

В публичные отчёты нельзя включать полные UUP-ссылки, ключи продукта, токены и персональные пути. Порядок сообщения об уязвимостях описан в [SECURITY.md](SECURITY.md).

## Диагностика

После ошибки программа сохраняет журнал и показывает путь к нему. При проблемах необходимо проверить:

1. доступность UUP dump и Microsoft CDN;
2. свободное место на системном и рабочем дисках;
3. отсутствие блокировки `aria2`, DISM или конвертера антивирусом;
4. журнал конкретного запуска.

## Сборка

Проект не требует компиляции. Исходный код запускается через `Start-Builder.cmd` или `Start-Builder.ps1`. PowerShell-модуль находится в `src/WindowsISOBuilder/`.

## Тестирование

Репозиторий содержит Pester-тесты и конфигурацию PSScriptAnalyzer:

```powershell
powershell.exe -NoProfile -File .\tests\Run-Tests.ps1
```

CI настроен для Windows PowerShell 5.1 и PowerShell 7. В текущей среде эти проверки и полная ISO-сборка не запускались; их наличие в репозитории не является подтверждением успешного прохождения.

## Документация

- [Требования](REQUIREMENTS.md)
- [Архитектура](docs/ARCHITECTURE.md)
- [Статус реализации](docs/IMPLEMENTATION_STATUS.md)
- [Миграция исходной v4](docs/SOURCE_V4_MIGRATION.md)
- [История изменений](CHANGELOG.md)
- [Участие в разработке](CONTRIBUTING.md)

## Участие в разработке

Изменения выполняются в отдельной ветке и отправляются через pull request. Перед публикацией необходимо проверить совместимость с PowerShell 5.1, отсутствие фиксированного каталога Windows и соответствие пользовательских заявлений фактически выполненным тестам.

## Происхождение и благодарности

UUP dump предоставляет каталог, метаданные и пакет конвертации. Файлы Windows загружаются средствами этого пакета с серверов Microsoft Windows Update/CDN.

Проект не связан с Microsoft, не распространяет готовые ISO, не активирует Windows и не обходит лицензирование.

## Ограничения

- полный цикл сборки пока не подтверждён на Windows 10 и Windows 11;
- совместимость комбинаций virtual editions зависит от выбранной базовой редакции и версии конвертера UUP dump;
- внешний API и формат конвертационного пакета могут измениться;
- GUI и автоматическое обновление приложения отсутствуют;
- в неинтерактивном режиме пока нельзя указать UUID напрямую.

## Лицензия

Публичная лицензия пока не выбрана. До добавления файла `LICENSE` исходный код не следует считать свободно распространяемым или разрешённым для повторного использования.
