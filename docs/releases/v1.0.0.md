# Windows ISO Builder v1.0.0 — стабильный релиз

## Главное

Windows ISO Builder v1.0.0 — первый стабильный релиз GUI и PowerShell-клиента для сборки Windows 10/11 ISO через UUP dump. Он выпущен из принятого `v0.3.5-rc.1` и не добавляет новые product features относительно RC.

## Основные возможности

- WPF GUI поверх существующего PowerShell backend;
- динамический UUP dump catalog без зашитых Windows release/build catalogs;
- Windows 10/11, recommended build и full Catalog;
- выбор language, editions, multi-edition, WIM/ESD и существующих converter options;
- structured preflight до UAC;
- elevated build, progress, cancellation, logs, result actions и SHA-256 результата;
- RU/EN localization, English fallback, System/Light/Dark theme;
- sanitized diagnostics bundle;
- GitHub Issue Forms и in-app feedback;
- Stable/Prerelease update discovery через официальный GitHub Releases;
- global Network Policy: System, Direct, Custom HTTP, Custom SOCKS5;
- DPAPI CurrentUser для saved proxy password;
- fail-closed Custom proxy mode и loopback bridge для generated downloader/aria2.

## Что изменилось относительно pre-1.0

Стабильный релиз собирает в один baseline возможности pre-1.0 train: GUI, Backend Contract v1, BuildPlan v1, preflight/cancellation, feedback/update delivery, Network/Proxy, diagnostics redaction, release package safety и validation tooling.

Финальный v1.0.0 pass меняет только release metadata и публичную документацию:

- ApplicationVersion: `1.0.0`;
- GUI Version/FileVersion: `1.0.0` / `1.0.0.0`;
- PowerShell ModuleVersion остаётся `0.3.0`;
- Backend Contract SchemaVersion остаётся `1`;
- BuildPlan SchemaVersion остаётся `1`.

## Требования

- Windows 10/11 x64;
- доступ к UUP dump и Microsoft CDN;
- достаточно места на диске для UUP cache, work directory и ISO;
- UAC для privileged build stage;
- для release ZIP отдельный .NET Runtime не требуется;
- для сборки из source нужен .NET 10 SDK.

## Быстрый запуск

1. Скачайте `windows-iso-builder-v1.0.0.zip`.
2. Проверьте checksum файлом `windows-iso-builder-v1.0.0.zip.sha256`.
3. Полностью распакуйте ZIP.
4. Запустите `WindowsISOBuilder.exe` обычным пользователем.
5. Выберите Windows 10/11, язык, редакции, ESD/WIM и output directory.
6. Запустите preflight.
7. Нажмите «Создать ISO» и подтвердите UAC, если backend запросит повышение прав.

## Network / proxy

Настройки сети находятся в `Настройки -> Сеть`.

- System использует системное proxy-поведение Windows/.NET.
- Direct явно обходит proxy.
- Custom HTTP использует указанный HTTP proxy.
- Custom SOCKS5 использует SOCKS5 через локальный loopback bridge.

Custom mode fail-closed: ошибка конфигурации, credential или подключения не превращается в silent Direct fallback. Для generated downloader/aria2 upstream proxy host/user/password не передаются в command line.

## Безопасность

Приложение не содержит GitHub PAT/OAuth token, не отключает UAC/antivirus и не выполняет self-update. Diagnostics создаются вручную, используют allowlist и sanitizer; перед публикацией diagnostic ZIP всё равно нужно просмотреть.

Proxy password не хранится в `network.json`, BuildPlan, backend request, command line, logs, diagnostics или release artifact. Saved credential защищается Windows DPAPI CurrentUser.

## Проверка checksum

```powershell
$zip = Resolve-Path .\windows-iso-builder-v1.0.0.zip
$checksumFile = Resolve-Path .\windows-iso-builder-v1.0.0.zip.sha256
$actual = (Get-FileHash -Algorithm SHA256 -LiteralPath $zip).Hash.ToUpperInvariant()
$expected = ((Get-Content -LiteralPath $checksumFile -Raw).Trim() -split '\s+')[0].ToUpperInvariant()
if ($actual -ne $expected) { throw "Release SHA-256 mismatch." }
```

## Известные ограничения

- Автоматического self-update нет: приложение только открывает official GitHub release page.
- Installer/MSIX, USB writer/Rufus integration, History/Profiles, customization, debloat, unattended setup, driver injection, TPM bypass, activation, accounts/cloud sync, telemetry и plugins не входят в v1.0.0.
- Manual proxy acceptance, controlled generated-downloader proxy acceptance, Explorer/taskbar icon, high-DPI visual check и full packaged GUI ISO E2E имеют отдельные статусы в validation matrix и не считаются PASS без фактического выполнения.

## Обратная связь и обновления

В `О приложении` доступны GitHub Issue Forms для bug report и feature request. `Настройки -> Обновления` выполняет ручную проверку официальных GitHub Releases по выбранной Network Policy.

English documentation: [README_EN.md](../../README_EN.md) and [English release notes](v1.0.0_EN.md).
