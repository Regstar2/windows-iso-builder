# Windows ISO Builder v0.3.0-alpha.1 — GUI MVP

Дата: 2026-08-18

## Главное

`v0.3.0-alpha.1` добавляет первый пользовательский Windows GUI поверх стабилизированного PowerShell backend. Backend не переписан: WPF-клиент использует только `Invoke-WibBackend.ps1`, Backend Contract Schema v1 и BuildPlan Schema v1.

## Добавлено

- WPF GUI на C# / .NET 10 (`net10.0-windows`);
- Quick Mode для Windows 11 и Windows 10 через backend `GetRecommendedBuild`;
- Catalog Mode через `SearchBuilds` с архитектурой, Preview и display-фильтром servicing records;
- динамические языки и редакции из backend;
- multi-edition выбор, ESD/WIM и существующие build options;
- `CreateBuildPlan` и отдельный preflight UX;
- асинхронный `ExecuteBuildPlan` с incremental NDJSON event reader;
- cooperative cancellation через `CancelBuild`;
- success/error/cancelled states, открытие журналов/папки и копирование SHA-256;
- GUI logging в `%LOCALAPPDATA%\WindowsISOBuilder\logs`;
- C# unit/integration test project;
- `tools/Build-Gui.ps1`;
- self-contained `win-x64` publish и package smoke `WindowsISOBuilder.exe --backend-smoke`;
- GUI architecture documentation RU/EN.

## Совместимость

- ApplicationVersion: `0.3.0-alpha.1`;
- PowerShell ModuleVersion: `0.3.0`;
- Backend Contract SchemaVersion: `1`;
- BuildPlan SchemaVersion: `1`;
- существующие `Start-Builder.cmd`, `Start-Builder.ps1`, TUI и CLI сохранены;
- GUI запускается как обычный пользователь (`asInvoker`), elevation остаётся ответственностью backend.

## Архитектурная граница

GUI не импортирует PowerShell module напрямую, не вызывает private functions, не парсит `Write-Host`, transcript, aria2/converter output и не управляет aria2/DISM/process tree самостоятельно. Типы ошибок определяются по `error.code`; совместимость — по `contractSchemaVersion`.

## Release package

Пользовательский ZIP должен содержать self-contained `win-x64` GUI вместе с существующим PowerShell backend/TUI/CLI. Установка отдельного .NET Runtime пользователю не требуется. Installer/MSIX в этой версии отсутствует.

## Validation status

В среде агента Windows/.NET/PowerShell validation не выполнялась, поэтому никакие PASS не заявляются без фактического запуска.

Перед merge/release владелец должен выполнить:

```powershell
dotnet restore .\WindowsISOBuilder.sln
dotnet build .\WindowsISOBuilder.sln -c Release
dotnet test .\WindowsISOBuilder.sln -c Release --no-build
powershell.exe -NoLogo -NoProfile -ExecutionPolicy Bypass -File .\tests\Run-Tests.ps1
powershell.exe -NoLogo -NoProfile -ExecutionPolicy Bypass -File .\tools\Build-Gui.ps1
powershell.exe -NoLogo -NoProfile -ExecutionPolicy Bypass -File .\tools\Invoke-ReleaseValidation.ps1 -Full
```

Manual GUI smoke: **NOT RUN**.

Real GUI E2E Windows 11 x64 ru-RU Professional ESD: **NOT RUN**.

## Не входит в версию

History, profiles, queue, cache-management GUI, updater, installer/MSIX, USB writer/Rufus, full theme/language settings, account/cloud features, Windows customization/debloat, drivers, TPM bypass, activation, custom downloader/converter и GitHub Actions.
